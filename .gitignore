.env
*.pyc
__pycache__/
.DS_Store
*.log

# Reversal payloads for writes we've done — may contain lead PII, keep off
# GitHub. Directory itself is committed (with README.md) so the pattern is
# discoverable; the JSON payloads inside are ignored.
scratchpad-persistent/*
!scratchpad-persistent/README.md
